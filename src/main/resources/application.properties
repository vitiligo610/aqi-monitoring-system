# --- Server Configuration ---
server.port=8080

# --- Database Configuration (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5433/aqi_db
spring.datasource.username=postgres
spring.datasource.password=asdf1234
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
# 'update' is fine for development. For production, you must use a migration tool like Flyway.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Gmail/Email Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lostunknown06@gmail.com
spring.mail.password=xnzoxpgnskwkudbd
# ^^^ IMPORTANT: This must be a "Google App Password", not your real password.
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Custom Application Properties ---
# 1. Generate a strong, random string for your JWT secret
app.jwt.secret=SIRIUS_AURA_AQI_2025_SC
app.jwt.expiration-ms=86400000
# 24 hours

# 2. This is where you will put your OpenWeatherMap key
app.openweathermap.api-key=cdce0298b8b36d521aa63f270c9bc4db

